## Scripts

This folder is for one-off helper scripts, experiments, and external API
examples that are not part of the main recommendation pipeline.

Current contents:

- `openrouter_stream_example.py`: small streaming API example

Use `pipeline/` for the reproducible recommender workflow.
